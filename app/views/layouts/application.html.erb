<!DOCTYPE html>
<html>
  <head>
    <title>Beerkeeper</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= favicon_link_tag('favicon.ico') %>
    <%= stylesheet_link_tag("primer_view_components") %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag("primer_view_components") %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <%= link_to root_path, class: 'navbar-brand' do %>
        <%= image_tag "logo.svg", width: '180' %>
      <% end %>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto d-flex flex-row-reverse w-100">
          <li class="nav-item active">
            <% if current_user %>
              <%= link_to 'ログアウト', session_path(current_user), data: { turbo_method: :delete } %>
            <% else %>
              <%= link_to 'ログイン', login_path %> | <%= link_to 'サインアップ', signup_path %>
            <% end %>
          </li>
        </ul>
      </div>
    </nav>

    <div class="container">
      <% flash.each do |message_type, message| %>
        <%= message_type %>
        <%= render(Primer::Beta::Flash.new(scheme: message_type.to_sym, dismissible: true)) { message } %>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
